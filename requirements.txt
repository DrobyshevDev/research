# Everything needed to build the site and check the notes.
# Deliberately small: nothing here is published, so the only dependency that
# earns its place is one the site or the contract needs.

mkdocs-material>=9.7.7
mkdocs-static-i18n>=1.3.1      # a second language is one file per page, not a fork

# check_notes.py parses front matter with it. Already present as a transitive
# dependency of mkdocs; named here because the script imports it directly and a
# transitive dependency is not a promise.
PyYAML>=6.0.3
